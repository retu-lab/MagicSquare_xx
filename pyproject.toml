[project]
name = "magicsquare-xx"
version = "0.1.0"
description = "4×4 마방진 10선(행·열·대각선) 합 검증 — validate_lines"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
